@page "/surat-masuk"
@attribute [Authorize]
@using AplikasiSurat.Models
@using AplikasiSurat.Services
@inject MailService MailService
@inject NavigationManager Navigation
@rendermode InteractiveServer

<PageTitle>Surat Masuk</PageTitle>

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-0">Surat Masuk</h2>
            <p class="text-muted small">Daftar semua surat yang masuk ke sistem</p>
        </div>
        <a href="surat-masuk/baru" class="btn btn-primary">
            <i class="bi bi-plus-lg me-2"></i>Tambah Surat
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4">Tgl Masuk</th>
                            <th>No. Surat</th>
                            <th>Pengirim</th>
                            <th>Perihal</th>
                            <th>File</th>
                            <th class="text-end pe-4">Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (suratList == null)
                        {
                            <tr><td colspan="6" class="text-center py-5">
                                <div class="spinner-border text-primary" role="status"></div>
                            </td></tr>
                        }
                        else if (!suratList.Any())
                        {
                            <tr><td colspan="6" class="text-center py-5 text-muted">Belum ada data surat masuk.</td></tr>
                        }
                        else
                        {
                            @foreach (var item in suratList)
                            {
                                <tr>
                                    <td class="ps-4">@item.TanggalMasuk.ToString("dd MMM yyyy")</td>
                                    <td><span class="badge bg-light text-dark border">@item.NomorSurat</span></td>
                                    <td>@item.Pengirim</td>
                                    <td>@item.Perihal</td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.FilePath))
                                        {
                                            <a href="@item.FilePath" target="_blank" class="btn btn-sm btn-outline-primary">
                                                <i class="bi bi-file-earmark-text"></i> Unduh
                                            </a>
                                        }
                                        else
                                        {
                                            <span class="text-muted small">-</span>
                                        }
                                    </td>
                                    <td class="text-end pe-4">
                                        <div class="btn-group">
                                            <a href="surat-masuk/edit/@item.Id" class="btn btn-sm btn-light text-primary border" title="Edit">
                                                <i class="bi bi-pencil-square"></i>
                                            </a>
                                            <button @onclick="() => Delete(item.Id)" class="btn btn-sm btn-light text-danger border" title="Hapus">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@code {
    private List<SuratMasuk>? suratList;

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
    }

    private async Task LoadData()
    {
        suratList = await MailService.GetSuratMasukAsync();
    }

    private async Task Delete(int id)
    {
        await MailService.DeleteSuratMasukAsync(id);
        await LoadData();
    }
}
